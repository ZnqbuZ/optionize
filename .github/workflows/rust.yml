name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: 1.95
    - uses: taiki-e/install-action@v2
      with:
        tool: cargo-hack
    - name: Check all feature combinations
      run: cargo hack check -p optionize --feature-powerset --locked
    - name: Test all feature combinations
      run: cargo hack test -p optionize --feature-powerset --locked
