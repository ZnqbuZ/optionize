[workspace]
resolver = "2"
members = [
    "optionize", "optionize-macros", "optionize/tests/fixtures/models"
]

[workspace.package]
repository = "https://github.com/ZnqbuZ/optionize"
version = "0.5.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"
keywords = ["builder", "config", "partial", "macro", "derive"]
categories = ["config", "rust-patterns"]

[workspace.dependencies]
optionize-macros = { version = "0.5.0", path = "optionize-macros" }

[profile.dev]
panic = "unwind"
debug = 2

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = 3
strip = true
